import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontWeight: "bold",
  },
  starContainer: {
    flexDirection: "row",
  },
  reviewField: {
    borderRadius: 10,
    borderWidth: 1,
    height: 100,
    paddingHorizontal: 5,
  },
})
